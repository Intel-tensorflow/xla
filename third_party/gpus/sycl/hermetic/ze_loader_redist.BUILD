package(default_visibility = ["//visibility:public"])

# ze_loader shared libraries (e.g., libze_loader.so*, .dll, .dylib)
filegroup(
    name = "l0_lib",
    srcs = glob(["lib/**"], allow_empty = False),
)
